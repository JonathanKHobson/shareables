# GitHub Pages Candidate

Upload this folder as a static site if GitHub Pages is selected for temporary Shopify image hosting.

The direct image URL pattern should be:

`https://<user-or-org>.github.io/<repo>/images/<safe_hosted_filename>`

Verify a sample of image URLs in a browser before using them in Shopify.
